package com.aubay.challenge.backend;

/*
 * @RunWith(SpringRunner.class)
 * 
 * @SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.MOCK)
 * 
 * @AutoConfigureMockMvc
 * 
 * @TestPropertySource(locations = "classpath:application-integrationtest.properties") public class
 * MovieRestContollerIntegrationTest {
 * 
 * @Autowired private MockMvc mvc;
 * 
 * @Autowired private MovieRepository repository;
 * 
 * }
 */
